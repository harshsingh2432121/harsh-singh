int birthdayCakeCandles(vector<int> candles) {
int max=candles[0];
    int n=candles.size();
    for(int i=0;i<n;i++){
        if(candles[i]>max)
            max=candles[i];
    }
    int ans=0;
    for(int i=0;i<n;i++)
        if(candles[i]==max)
        {
            ans++;
        }
    return ans;
}
