int jumpingOnClouds(vector<int> c, int k) {
 int n=c.size();
    int i=0,ans=100;
    do
    {
        if(c[i]==0){
            ans=ans-1;
        }
        else if (c[i]==1)
        {
            ans=ans-3;
        }
       i=(i+k)%n;
    }while(i!=0);
    return ans;

}
